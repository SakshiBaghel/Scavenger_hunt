import PhotoUpload from "../components/PhotoUpload"; // Correct import
const UploadPhoto = () => {
  return (
    <div>
      <h2>Upload Photo Page</h2>
      <PhotoUpload /> {/* Using the component */}
    </div>
  );
};
export default UploadPhoto;
